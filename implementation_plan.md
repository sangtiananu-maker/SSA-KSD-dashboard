# แผนการบูรณาการโมเดล Gemma 3 270M เข้าสู่ Dashboard (In-Browser WebGPU + ระบบสมัครใจดาวน์โหลด)

สถาปัตยกรรมนี้ถูกออกแบบตามความต้องการของผู้ใช้: **"ใช้ In-Browser AI ผ่าน WebGPU / Transformers.js รันสดในเบราว์เซอร์ 100% โดยไม่ต้องมี Python Server และใช้ระบบสมัครใจ (Opt-in) มีปุ่มกดยืนยันก่อนเริ่มดาวน์โหลด Cache โมเดล"**

---

## 1. ลำดับประสบการณ์ผู้ใช้งาน (User Experience & Opt-in Flow)

```
[ผู้ใช้เข้าเว็บ Dashboard] (index.html บน GitHub Pages / มือถือ / Desktop)
       │
       ▼
[หน้าเว็บโหลดเร็วทันใจ 0.05 วินาที] 
* ไม่มีการดาวน์โหลดโมเดลล่วงหน้า (0 MB AI Download)
* ข้อมูลยอดขาย, ตาราง 181 SKUs, ชาร์ต และการจัดกลุ่มแบรนด์ (0 Other) ใช้งานได้ปกติทันที 100%
* มีปุ่มเด่นชัด: "🤖 Gemma 3 AI Copilot (WebGPU In-Browser)"
       │
       ▼ (ผู้ใช้กดปุ่ม AI Copilot)
[ระบบตรวจสอบ Browser Cache / IndexedDB]
       │
       ├──────────────────────────────────────────┐
       ▼ (กรณี: ยังไม่เคยดาวน์โหลดโมเดล)           ▼ (กรณี: เคยดาวน์โหลดและมี Cache แล้ว)
[แสดงหน้าต่างเตือนและขอความยินยอม (Consent Modal)]  [เปิดหน้าต่าง AI Copilot ทันที]
- ชี้แจงขนาดไฟล์: ~140 MB (ดาวน์โหลดครั้งเดียว)     - ขึ้นสถานะ: "🟢 Gemma 3 พร้อมใช้งาน (Offline Ready)"
- แจ้งข้อดี: ประมวลผลบนเครื่อง 100%, ไร้ Server,   - พร้อมพิมพ์แชทหรือกดปุ่มวิเคราะห์สดได้ทันที
  ทำงานแบบออฟไลน์ได้หลังดาวน์โหลด
- คำแนะนำ: แนะนำให้ต่อ Wi-Fi ก่อนโหลด
       │
       ├─────────────────────┐
       ▼ (กด "ยกเลิก")        ▼ (กดยืนยัน "ดาวน์โหลดและเปิดใช้งาน")
[ปิดหน้าต่างกลับสู่แดชบอร์ด]  [แสดง Progress Bar ดาวน์โหลด]
                             - แสดง % และขนาด MB ที่โหลดแบบเรียลไทม์
                             - บันทึกลง IndexedDB / CacheStorage อัตโนมัติ
                             - เมื่อเสร็จ 100% -> สลับเข้าสู่หน้าต่างแชท AI ทันที
```

---

## 2. โครงสร้างทางเทคนิค (Technical Architecture)

1. **Engine**: `@huggingface/transformers` (Transformers.js v3)
2. **Model**: `onnx-community/gemma-3-270m-it-ONNX` (เวอร์ชัน Quantized 4-bit ขนาด ~140 MB)
3. **Execution Device**: `device: "webgpu"` พร้อม fallback เป็น `wasm` หากเครื่องไม่มี WebGPU
4. **Cache Storage**: ใช้มาตรฐานเบราว์เซอร์ `IndexedDB` / `CacheStorage` เก็บน้ำหนักโมเดลถาวร
5. **Zero Server**: ไม่ต้องมี Python Server, ไม่ต้องมี Cloud API Key รันในแท็บเบราว์เซอร์ของลูกค้าโดยตรง 100%
6. **ปุ่มจัดการ Cache**: มีปุ่ม "ล้างข้อมูลโมเดล (Clear AI Cache)" เผื่อผู้ใช้ต้องการคืนพื้นที่หน่วยความจำ ~140 MB ให้เบราว์เซอร์

---

## 3. ฟังก์ชันอัจฉริยะที่ Gemma 3 Copilot จะให้บริการ

1. **📊 Executive Monthly Briefing (วิเคราะห์ยอดขายเดือน ส.ค. 2026 สดๆ)**:
   - ป้อนข้อมูลสรุปยอดขาย 6 สาขา และกลุ่มสินค้าเข้า Context window ของ Gemma 3
   - โมเดลจะเขียนบทวิเคราะห์ภาษาไทย อธิบายจุดแข็ง จุดอ่อน และสินค้า Hero แบบเป็นธรรมชาติ
2. **🏪 Store Visit Briefing (เตรียมข้อมูลตรวจสาขารายบุคคล)**:
   - ผู้บริหารเลือกสาขา (เช่น โพนสีนวน หรือ ดงโดก)
   - Gemma 3 วิเคราะห์สินค้าที่สาขานั้นทำยอดได้ดี และชี้เป้าสินค้ากลุ่ม Strategic ที่ยังไม่เคยมียอดขายในสาขานี้ (White-Space Opportunity) เพื่อนำไปคุยกับผู้จัดการร้าน
3. **💬 Dynamic Free-Form Medical & Sales Q&A**:
   - ผู้บริหารหรือผู้ใช้สามารถพิมพ์ถามคำถามอิสระได้ เช่น:
     - *"ช่วยเปรียบเทียบยอดขายกลุ่มไพลวาน่า ระหว่างสาขาดงโดก กับ สาขาโพนสีนวน ให้หน่อย"*
     - *"ยาตัวไหนในกลุ่มแก้ปวดและลดไข้ที่เหมาะจะดันยอดขายเพิ่ม"*
4. **🏷️ Core Brand Grouping & Smart Search (Baseline Level)**:
   - ฝัง Ontology 181 SKUs ลงในโค้ด JavaScript ให้จับกลุ่มแบรนด์ถูกต้อง 100% (0 "Other") และค้นหา 3 ภาษาได้ทันทีโดยไม่ต้องรอโหลดโมเดล

---

## 4. แผนการสร้างและแก้ไขไฟล์ (Proposed Changes)

### [NEW] [gemma_engine.js](file:///d:/Antigravity/SSA%20%E0%B8%A2%E0%B8%AD%E0%B8%94%E0%B8%82%E0%B8%B2%E0%B8%A2/gemma_engine.js)
- สคริปต์จัดการ In-Browser AI ผ่าน Transformers.js:
  - ฟังก์ชันตรวจสอบสถานะ Cache (`checkModelCached()`)
  - ฟังก์ชันดาวน์โหลดพร้อม Progress Callback (`downloadAndInitGemma(onProgress)`)
  - ฟังก์ชันรันโมเดลภาษาไทย (`generateResponse(prompt, streamCallback)`)
  - ฟังก์ชันล้าง Cache (`clearGemmaCache()`)

### [MODIFY] [index.html](file:///d:/Antigravity/SSA%20%E0%B8%A2%E0%B8%AD%E0%B8%94%E0%B8%82%E0%B8%B2%E0%B8%A2/index.html)
- เพิ่มปุ่มเรียกใช้งาน AI: `<button id="btn-gemma-copilot" class="gemma-trigger-btn">🤖 Gemma 3 AI Copilot</button>` ใน Navbar / Header
- เพิ่มโครงสร้าง Modal ขอความยินยอม (Opt-in Consent Modal) พร้อมปุ่มดาวน์โหลด/ยกเลิก
- เพิ่มโครงสร้างหน้าต่างสนทนา AI Copilot Drawer (Chat Interface + Quick Action Pills)

### [MODIFY] [styles.css](file:///d:/Antigravity/SSA%20%E0%B8%A2%E0%B8%AD%E0%B8%94%E0%B8%82%E0%B8%B2%E0%B8%A2/styles.css)
- ตกแต่งสไตล์สำหรับ Consent Modal, Progress Bar แสดงการดาวน์โหลดโมเดล
- ตกแต่งสไตล์สำหรับ Copilot Slide Drawer (แชทบับเบิ้ล, ปุ่มคำถามลัด, กล่องพิมพ์ข้อความ) รองรับทั้ง Desktop และมือถือ

### [MODIFY] [app.js](file:///d:/Antigravity/SSA%20%E0%B8%A2%E0%B8%AD%E0%B8%94%E0%B8%82%E0%B8%B2%E0%B8%A2/app.js)
- ปรับปรุงฟังก์ชัน `getBrandGroup()` ให้คลอบคลุม 181 SKUs ครบ 100% (0 "Other")
- เชื่อมต่อ Event Listener ของปุ่ม AI Copilot เข้ากับ `gemma_engine.js`
- ส่งสรุปตัวเลขสถิติปัจจุบันของแดชบอร์ดเข้าเป็นบริบท (Context) ของโมเดลเมื่อผู้ใช้กดปุ่มวิเคราะห์

---

## 5. ขั้นตอนการตรวจสอบและทดสอบ (Verification Plan)

1. **ทดสอบความเร็วและการไม่โหลดโมเดลล่วงหน้า (Zero Initial Overhead)**:
   - เปิดหน้าเว็บครั้งแรก ตรวจสอบ Network Tab ว่าไม่มีการดาวน์โหลดไฟล์โมเดล ~140 MB เลยจนกว่าจะกดปุ่ม
2. **ทดสอบระบบขอความยินยอม (Opt-in Modal)**:
   - กดปุ่ม AI Copilot -> ตรวจสอบว่ามีหน้าต่างเตือนขนาดไฟล์ ~140 MB แสดงขึ้นมาอย่างชัดเจน
   - กด "ยกเลิก" -> หน้าต่างปิดลง และไม่มีการโหลดไฟล์ใดๆ
3. **ทดสอบการดาวน์โหลดและ Progress Bar**:
   - กดยืนยันดาวน์โหลด -> ตรวจสอบว่าแถบ % และข้อความแสดงขนาดไฟล์ขยับตามจริง
4. **ทดสอบการจำ Cache ในเครื่อง**:
   - เมื่อโหลดเสร็จแล้ว ลองรีเฟรชหน้าเว็บแล้วกดปุ่ม AI Copilot อีกครั้ง -> ตรวจสอบว่าระบบตรวจพบ Cache ทันทีและเปิดหน้าแชทได้โดยไม่ต้องดาวน์โหลดใหม่
5. **ทดสอบการทำงานของ Gemma 3 In-Browser**:
   - ทดสอบกดปุ่มคำถามสำเร็จรูป เช่น "สรุปยอดขายเดือน ส.ค." หรือ "วิเคราะห์สาขาโพนสีนวน"
   - ทดสอบพิมพ์คำถามอิสระ -> ตรวจสอบการตอบของ Gemma 3 270M
6. **ทดสอบระบบจัดกลุ่มแบรนด์ 181 SKUs**:
   - ตรวจสอบตารางและกราฟแบรนด์ว่าไม่มีคำว่า "Other" หลงเหลืออยู่


